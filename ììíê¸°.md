# 시작하기 (사용자용)

흐름: **압축 풀기 → `/setup`(누락 패키지 설치 + GitHub 연결) → 자동으로 1~7단계 구현 → `/check`로 단계별 검증**

## 1. 압축 풀고 Claude Code 실행
```bash
cd ~ && unzip ~/Downloads/fire_ws_claude_kit.zip
# Claude Code 가 없으면 (처음 한 번)
curl -fsSL https://claude.ai/install.sh | bash     # 설치 후 새 터미널 열기
cd ~/fire_ws && claude
```

## 2. `/setup` 입력
Claude Code가 순서대로 진행하며, 아래 두 번은 **다른 터미널에서 직접** 실행해 달라고 요청합니다.

| 요청 | 실행할 명령 | 끝나면 Claude에 |
|---|---|---|
| 누락 패키지 설치 (설치된 건 건너뜀) | `cd ~/fire_ws && bash scripts/install_deps.sh` | `끝났어` |
| GitHub 로그인 (처음 한 번) | `gh auth login` → GitHub.com → HTTPS → Y → 브라우저 로그인 | `로그인 했어` |

처음에 저장소 이름·공개 범위를 물어봅니다. `기본값` 이라고 하면 `fire-patrol-sim`(private)로 만듭니다.
준비가 끝나면 **바로 STEP1~7 구현을 시작**합니다. 준비까지만 하려면 `/setup 여기까지만`.

## 3. 구현 중
- 단계마다 빌드 확인 → 커밋 → **GitHub에 자동 push** 됩니다.
- 중간에 끊기면 `/build-all` 을 다시 입력하면 이어서 합니다.

## 4. 단계별 검증
| 입력 | 동작 |
|---|---|
| `/check 1` | STEP1 검증: 실행 명령과 확인 포인트 안내 |
| (결과/에러 로그 붙여넣기) | 원인 찾아 수정 → 커밋·push → 다시 확인할 명령 안내 |
| `/pass 1` | STEP1 검증 완료 기록 |
| `/check 2` … `/pass 7` | 반복 |
| `/status` | 전체 현황 |

STEP4 검증 때 이미지 수집과 YOLO 학습(CPU 10~30분)을 직접 하게 됩니다.

## 5. 알아둘 것
- `install_deps.sh` 는 `sudo bash` 로 실행하지 마세요(내부에서 필요한 부분만 sudo 사용).
- 설치 상태만 보고 싶으면 `bash ~/fire_ws/scripts/check_deps.sh`
- Gazebo·RViz 창은 Claude가 알려준 명령을 별도 터미널에서 실행하세요.
- 시뮬이 꼬이면 `bash ~/fire_ws/scripts/kill_sim.sh`
- 슬래시 명령이 안 보이면 `docs/SETUP_PROMPT.md` 내용을 그대로 붙여넣어도 됩니다.
